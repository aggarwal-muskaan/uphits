export type TAuthTemplate = {
  form: JSX.Element;
};

export type TButton = {
  text: string;
};

export type TLogin = {
  email: string;
  password: string;
};

export type TSignup = {
  email: string;
  confirmEmail: string;
  newPassword: string;
  profileName: string;
};
